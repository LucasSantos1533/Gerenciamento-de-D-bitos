import 'package:flutter/material.dart';

class RelatoriosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Relatórios Financeiros')),
      body: Center(
        child: Text('Aqui serão exibidos os relatórios financeiros'),
      ),
    );
  }
}
